# com_ra_mailman — Instructions for Claude Code

This is the code repo for RA-Mailman, a Joomla component. It does not include the vault's persistent notes/graph.

Vault notes: `~/vault/ra-mailman/`
Knowledge graph: `~/vault/ra-mailman/graph/`

Query the graph first for structure/architecture questions rather than re-reading the whole codebase — see `~/vault/CLAUDE.md` for the 3-layer query rule and session commands (`/vault-resume`, `/vault-save`).
